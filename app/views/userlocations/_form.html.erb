<%= form_with(model: userlocation, local: true) do |form| %>
  <% if userlocation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(userlocation.errors.count, "error") %> prohibited this userlocation from being saved:</h2>

      <ul>
      <% userlocation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :lat %>
    <%= form.text_field :lat %>
  </div>

  <div class="field">
    <%= form.label :lng %>
    <%= form.text_field :lng %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
